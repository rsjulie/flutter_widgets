import 'package:flutter/material.dart';

class DataRowPage extends StatelessWidget {
  const DataRowPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DataRow'),
        centerTitle: true,
      ),
      body: Container(),
    );
  }
}
