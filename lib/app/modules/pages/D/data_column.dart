import 'package:flutter/material.dart';

class DataColumnPage extends StatelessWidget {
  const DataColumnPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DataColumn'),
        centerTitle: true,
      ),
      body: Container(),
    );
  }
}
