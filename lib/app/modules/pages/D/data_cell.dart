import 'package:flutter/material.dart';

class DataCellPage extends StatelessWidget {
  const DataCellPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DataCell'),
        centerTitle: true,
      ),
      body: Container(),
    );
  }
}
