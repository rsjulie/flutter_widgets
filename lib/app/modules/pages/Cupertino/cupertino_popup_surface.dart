import 'package:flutter/material.dart';

class CupertinoPopupSurfacePage extends StatelessWidget {
  const CupertinoPopupSurfacePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('CupertinoPopupSurface'),
        centerTitle: true,
      ),
      body: Container(),
    );
  }
}
