import 'package:flutter/material.dart';

class CupertinoTextFieldPage extends StatelessWidget {
  const CupertinoTextFieldPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('CupertinoTextField'),
        centerTitle: true,
      ),
      body: Container(),
    );
  }
}
