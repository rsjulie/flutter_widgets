import 'package:flutter/material.dart';

class CupertinoSegmentedControlPage extends StatelessWidget {
  const CupertinoSegmentedControlPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('CupertinoSegmentedControl'),
        centerTitle: true,
      ),
      body: Container(),
    );
  }
}
