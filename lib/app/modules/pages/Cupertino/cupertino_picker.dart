import 'package:flutter/material.dart';

class CupertinoPickerPage extends StatelessWidget {
  const CupertinoPickerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('CupertinoPicker'),
        centerTitle: true,
      ),
      body: Container(),
    );
  }
}
