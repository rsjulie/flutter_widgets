import 'package:flutter/material.dart';

class CupertinoSliderPage extends StatelessWidget {
  const CupertinoSliderPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('CupertinoSlider'),
        centerTitle: true,
      ),
      body: Container(),
    );
  }
}
